# Runtime deps for the DEPLOYED Streamlit app (Streamlit Community Cloud).
# Pinned to the versions the committed model pickles were built with, so they
# load correctly. Deploy on Python 3.12 (set in Streamlit Cloud advanced settings).
# Full pipeline / deck / demo tooling lives in requirements-dev.txt.
streamlit==1.37.1
numpy==1.26.4
pandas==2.3.0
scipy==1.13.1
scikit-learn==1.8.0
lightgbm==4.6.0
joblib==1.4.2
pydeck==0.8.0
plotly==5.24.1
pulp==3.3.2
requests==2.32.3
